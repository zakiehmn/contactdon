
class Contactdon:
    contactList

    def add_contact(fName,lName,emailAddress,phoneNumber):
        newContact=Contact()
        newContact.set_fName(fName)
        newContact.set_lName(lName)
        newContact.set_emailAddress(emailAddress)
        newContact.set_phoneNumber(phoneNumber)
        newContact.set_ID
        contactList.append(newContact)

        

